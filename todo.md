# 待完善功能

- [x] 优化构建项目 Webview 表单的主题适配。
   当前应用编码、`enginecode`、`h3_token` 等输入框使用浅色背景，在 VSCode 深色主题下观感不一致。后续可改为使用 VSCode Webview Theme CSS Variables，例如 `--vscode-input-background`、`--vscode-input-foreground`、`--vscode-input-border`，以自动适配用户当前主题。

- [x] git commit时，应该弹出输入框，默认值是：从氚云同步，但是允许用户修改。

- [x] 优化字段表
   字段表已包含控件类型、值类型、关联表单与关联多选中间表信息，并按主表、子表生成精简的 Markdown 表格。

- [x] 增加“查询氚云表单名称”编辑器右键菜单命令。
   当用户在编辑器中选中表单或子表编码时，在 Editor Context Menu 中显示“查询氚云表单名称”。命令通过氚云 SQL 接口查询对应主表名称并展示结果。

- [ ] 探索“编译代码到氚云”表单目录右键菜单命令。
   在 Explorer Context Menu 中为表单目录增加“编译代码到氚云”命令。点击后读取本地表单前端、表单后端、列表前端、列表后端代码，并尝试调用氚云接口保存到云端，以减少用户手动复制粘贴。该能力依赖氚云相关保存接口的可用性和稳定性，前期仅作为可行性探索，实现风险较高。

- [ ] 完成新版接口对表单数据和表单代码同步的适配。
   当前 `h3yunApiVersion` 设置为 `new` 时，仅列表前端和列表后端代码使用新版列表设计器接口；表单结构（`fields.md`）、表单前端代码和表单后端代码仍使用旧版 `SheetDesigner` 接口。后续需要确认新版对应接口及响应格式，并完成 `h3yunApiNew.ts` 中的适配。
